# Голосовой AI-ассистент

Веб-приложение для обработки голосовых запросов с использованием Whisper (распознавание речи), Ollama (LLM) и gTTS (синтез речи).

## Возможности

- 🎤 Распознавание речи из аудиофайлов (Whisper)
- 🤖 Генерация ответов с помощью LLM (Ollama)
- 🔊 Синтез речи в аудио (gTTS)
- 🐳 Запуск через Docker Compose
- 📡 REST API для интеграции

## Требования

- Docker и Docker Compose
- Или Python 3.11+ (для локального запуска)

## Быстрый старт

### Запуск с Docker Compose (рекомендуется)

1. Клонируйте репозиторий или перейдите в директорию проекта:
```bash
cd ai_prototype
```

2. Запустите приложение:
```bash
docker-compose up -d
```

3. Дождитесь загрузки модели Ollama (может занять несколько минут при первом запуске)

4. Приложение будет доступно по адресу: `http://localhost:8000`

5. Документация API доступна по адресу: `http://localhost:8000/docs`

### Остановка приложения

```bash
docker-compose down
```

Для полной очистки данных (включая модели):
```bash
docker-compose down -v
```

## Локальный запуск (без Docker)

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Установка системных зависимостей

**Windows:**
- Установите FFmpeg: https://ffmpeg.org/download.html
- Добавьте FFmpeg в PATH

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install -y ffmpeg libsndfile1
```

**Mac:**
```bash
brew install ffmpeg libsndfile
```

### 3. Запуск Ollama

Убедитесь, что Ollama запущен и доступен:
```bash
# Установите Ollama с https://ollama.ai
# Запустите Ollama
ollama serve

# В другом терминале загрузите модель
ollama pull llama3.2
```

### 4. Настройка переменных окружения (опционально)

Создайте файл `.env` в корне проекта:
```env
APP_NAME=Голосовой AI-ассистент
DEBUG=False

WHISPER_MODEL_SIZE=base
WHISPER_DEVICE=auto
WHISPER_COMPUTE_TYPE=auto

OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_TIMEOUT=120

TTS_ENABLED=true
TTS_LANGUAGE=ru

MAX_FILE_SIZE=25
```

### 5. Запуск приложения

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Приложение будет доступно по адресу: `http://localhost:8000`

## Использование API

### Проверка состояния сервисов

```bash
curl http://localhost:8000/health
```

### Обработка голосового запроса

```bash
curl -X POST "http://localhost:8000/voice/process" \
  -F "audio=@your_audio_file.wav" \
  -F "generate_audio=true" \
  -F "system_prompt=Ты вежливый помощник"
```

**Параметры:**
- `audio` (обязательный) - аудиофайл в формате WAV
- `generate_audio` (опционально, по умолчанию `true`) - генерировать ли аудиоответ
- `system_prompt` (опционально) - пользовательский системный промпт для LLM

**Ответ:**
```json
{
  "transcription": {
    "text": "распознанный текст",
    "language": "ru",
    "duration": 5.2
  },
  "llm_response": {
    "text": "ответ от LLM",
    "model": "llama3.2"
  },
  "audio_response": {
    "file_path": "outputs/response_xxx.mp3",
    "duration": 3.1
  }
}
```

## Структура проекта

```
ai_prototype/
├── app/
│   ├── api/
│   │   └── routes/          # API эндпоинты
│   ├── core/
│   │   └── dependencies.py   # Зависимости FastAPI
│   ├── models/
│   │   └── schemas.py        # Pydantic схемы
│   ├── services/             # Сервисы (Whisper, Ollama, TTS)
│   ├── config.py            # Настройки приложения
│   └── main.py              # Точка входа FastAPI
├── uploads/                  # Временные загруженные файлы
├── outputs/                  # Сгенерированные аудиоответы
├── docker-compose.yml        # Docker Compose конфигурация
├── Dockerfile               # Docker образ приложения
├── requirements.txt         # Python зависимости
└── README.md               # Этот файл
```

## Настройки

Основные настройки можно изменить через переменные окружения или в `app/config.py`:

- `WHISPER_MODEL_SIZE` - размер модели Whisper (`tiny`, `base`, `small`, `medium`, `large`)
- `OLLAMA_MODEL` - модель Ollama для использования
- `TTS_ENABLED` - включить/выключить синтез речи
- `TTS_LANGUAGE` - язык для синтеза речи
- `MAX_FILE_SIZE` - максимальный размер файла в МБ

## Проблемы и решения

### Ollama не запускается
- Убедитесь, что порт 11434 свободен
- Проверьте логи: `docker-compose logs ollama`

### Модель Whisper не загружается
- Проверьте подключение к интернету (первая загрузка)
- Убедитесь, что достаточно места на диске

### Ошибки при обработке аудио
- Убедитесь, что файл в формате WAV
- Проверьте размер файла (максимум 25 МБ по умолчанию)

## Разработка

Для разработки с автоматической перезагрузкой:

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

## Лицензия

Этот проект является прототипом для демонстрации возможностей.
